# MARTA train API key — request at https://itsmarta.com/app-developer-resources.aspx
MARTA_TRAIN_KEY=

# Bluesky
BLUESKY_SERVICE=https://bsky.social
BLUESKY_BUS_IDENTIFIER=
BLUESKY_BUS_APP_PASSWORD=
BLUESKY_TRAIN_IDENTIFIER=
BLUESKY_TRAIN_APP_PASSWORD=
# Alerts/disruptions account (martaalertinsights) — used by bin/marta/alerts.js,
# bin/marta/incident-roundup.js, bin/marta/bus/cancellations.js, and the bus/rail
# thin-gap + pulse bins.
BLUESKY_ALERTS_IDENTIFIER=
BLUESKY_ALERTS_APP_PASSWORD=

# Throwaway Bluesky account for manually testing post output without publishing
# to a live bot account. Optional — no automated job reads these.
TEST_ACCOUNT_IDENTIFIER=
TEST_ACCOUNT_PASSWORD=

# Mapbox — https://account.mapbox.com/access-tokens/
MAPBOX_TOKEN=

# GitHub PAT used by bin/marta/push-web-data.sh to fire a repository_dispatch that
# rebuilds the public site after data is uploaded to R2. Fine-grained token with
# Contents:write on cailinpitt/atlanta-transit-alerts. Keep the value unquoted —
# the script greps it out of this file. Optional: if unset, the upload still
# happens and the site's scheduled build is the catch-up.
GITHUB_DISPATCH_TOKEN=
